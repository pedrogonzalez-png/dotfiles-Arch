-- 1. Garante que o motor de blur está ativo globalmente
hl.config({
    decoration = {
        blur = {
            enabled = true,
            size = 3,
            passes = 2,
            ignore_opacity = true,
            new_optimizations = true,
        }
    }
})

-- 2. Regra Universal: TODOS os apps (incluindo o Kitty) ficam 100% sólidos 
-- quando você os usa, e ganham transparência + blur quando ficam em segundo plano.
hl.window_rule({
    match = { class = ".*" },
    no_blur = false,
    opacity = "1.0 override 0.85 override"
})


























































































































































































































































































































